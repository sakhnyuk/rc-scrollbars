import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'rc-scrollbars',
  mode: 'site',
  logo: '/logo.svg',
  favicon: '/favicon.ico',
  exportStatic: {},
  ssr: {},
  hash: true,
});
